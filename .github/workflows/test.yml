name: Test dd

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          chmod +x build.sh
          ./build.sh
      
      - name: Run
        run: |
          docker run -i alpine /bin/ash -c 'cat LICENSE'
          